# Disable sudo to enable containerization
sudo: false

# Setup Go language support
language: go

# Test against multiple versions of Go
go:
  - 1.8.x
  - master

# Install dependencies
before_install:
  - go get -t -v ./... # TODO: What does this actually do, apart from printing the package name?
  - go get github.com/goreleaser/goreleaser # TODO: Do we need this or does the above install the dependencies for us?

# Generate code coverage
install:
  - go vet
  - golint
  - go test -race -coverprofile=coverage.txt -covermode=atomic

# Build binaries
script:
  - go vet
  - golint
  - go test -tags=integration
  - go build

# Upload code coverage and release binaries
after_success:
  - bash <(curl -s https://codecov.io/bash)
  - test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash
